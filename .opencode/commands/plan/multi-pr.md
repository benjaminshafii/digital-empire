---
description: Design an atomic PR strategy for a PRD
---
You are running in the digital-empire control center. Create an atomic PR strategy for:

$ARGUMENTS

Requirements:
1) Draft a sequence of independent PRs that do not step on each other.
2) Each PR should be shippable on its own, with clear scope and touchpoints.
3) Note dependencies, ordering, tests, and rollback considerations.
4) Write the plan to `research/<slug>-multi-pr.md` using a kebab-case slug from $ARGUMENTS.

Output:
- Provide a short status log and the strategy file path.
