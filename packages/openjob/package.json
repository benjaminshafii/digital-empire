{
  "name": "openjob",
  "version": "1.0.0",
  "description": "OpenCode plugin for scheduling recurring prompts via launchd/systemd",
  "type": "module",
  "scripts": {
    "install": "bash install.sh"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
